import { IPlayer } from '../interface/IPlayer';

export default function Battle(props: { player: IPlayer }) {
  return (
    <div
      style={{
        display: 'flex',
      }}
    >
      player
    </div>
  );
}
